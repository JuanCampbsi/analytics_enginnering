version: 2

models:
    - name: ibm_prices_bronze
      description: "Camada de bronze resultados da IBM"
      columns:
          - name: datetime
            description: "Data e hora do resultado"
            tests:
              - unique
              - not_null